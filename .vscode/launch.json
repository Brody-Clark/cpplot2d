{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug (macOS/LLDB)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/build/test_plot_mac",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build",
        }
    ]
}